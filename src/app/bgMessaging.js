// @flow
import firebase from '@react-native-firebase/app';
// Optional flow type
import type { RemoteMessage } from 'react-native-firebase';

export default async (message: firebase) => {
  // handle your message

  return Promise.resolve();
}
